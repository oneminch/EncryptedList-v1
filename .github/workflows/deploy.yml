name: Deployment

on:
   push:
      branches: [master]

jobs:
   build:
      runs-on: ubuntu-latest
      steps:
         - uses: actions/checkout@v2
         - uses: JamesIves/github-pages-deploy-action@releases/v3
         - run: npm run build
